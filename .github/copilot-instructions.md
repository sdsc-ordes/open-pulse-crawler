# Copilot project instructions

# Environment

This project uses Python 3.13 with uv. Please use the commands wisely. The development should have already installed the necessary dependencies in the virtual environment, if not use `uv pip install ...` or `uv pip install -e .`.